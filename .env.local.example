# Trading Strategy
# mega_only, conservative, balanced, aggressive, reversal_only, mean_reversion, scalping
STRATEGY=balanced

# Risk Management
RISK_PER_TRADE=100
MAX_RISK_PER_SHARE=2.00
MIN_RR_RATIO=1.3

# Filters (comma-separated; empty means "allow all")
ALLOWED_TICKERS=SPY,QQQ,AAPL,TSLA
ALLOWED_TIMEFRAMES=10,15

# Vercel KV (auto-added in Vercel when you connect KV)
KV_URL=
KV_REST_API_URL=
KV_REST_API_TOKEN=
KV_REST_API_READ_ONLY_TOKEN=
